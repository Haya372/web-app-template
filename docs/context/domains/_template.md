> このファイルを `<entity>.md` にコピーして使用してください。書く内容と書かない内容は [`../README.md`](../README.md) を参照してください。

# <ドメイン名>

## 課題 / 目的

このドメインが解決しようとしている問題を 2〜5 文で記載する。

- 誰の・どんな課題を解決するか
- 解決された結果として何ができるようになるか

## 業務ルール / 制約

変わりにくい概念レベルの制約のみを記載する。実装詳細ではなくビジネスルールを書く。

- 例: 注文は確定後にキャンセルできない（キャンセル要求は別の「キャンセル申請」エンティティとして扱う）
- 例: 在庫数は 0 未満にならない
- 例: ユーザーは 1 つだけアクティブなセッションを持てる

## 状態遷移（任意 — ステータスを持つドメインのみ）

ステータスと遷移契機をコードに依存しない概念レベルで記載する。

```
draft → submitted → approved → closed
                  → rejected
```

## 用語（このドメイン固有のもの）

`glossary.md` に掲載済みの共通用語はここに重複させない。

| 用語 | English | 定義 |
|------|---------|------|
| （例: 仮注文） | （例: Draft Order） | （例: 確定前の編集可能な注文） |

## 関連

このドメイン固有の設計判断がある場合のみ ADR を参照する。
すべてのドメインが従う横断的な方針（Clean Architecture など）の記載は不要。

- 掲載する例: このドメインの集約境界をどう決めたかを記録した ADR
- 掲載しない例: 全ドメイン共通の Clean Architecture ADR

---

- 関連 ADR: `docs/decisions/ADR-XXXX-...md`
- 関連コード: `go-backend/internal/domain/<entity>/`
- 関連テスト: `go-backend/internal/domain/<entity>/<entity>_test.go`
