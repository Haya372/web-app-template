以下の内容をもとに、`docs/guidelines/task-progress-guide.md` のテンプレートに従ってチケットを作成してください。

必要に応じてコードベース（ADR、ガイドライン、関連ファイル）を参照し、要件・設計セクションを具体化してください。

**ユーザーの説明:**
$ARGUMENTS

---

## チケット作成ルール

関係しないセクションは省略してよいが、省略する場合は理由を添えること。

### テンプレート

**サマリ**
- 1 行でタスクの意図を説明

**背景 / 課題**
- どのような状況で何が問題になっているかを Fact ベースで記載

**目的 / 成功基準**
- "〇〇ができる状態" のように達成後の姿を宣言
- 主要な KPI や期待値 (例: レスポンス時間 < 200ms) があれば合わせて記載

**スコープ**
- 対象となる機能やコンポーネント
- 対象外・やらないことも明示

**要件 / 設計**

<!-- Backend が関係する場合 -->
**Backend**
- エンドポイント: `<METHOD> /path`
- Headers:
  | Header | 必須 | 値 | 備考 |
  | --- | --- | --- | --- |
- Body (JSON サンプル)
  ```json
  {
    "field": "value"
  }
  ```
- Response (200) サンプル JSON
- Errors: 400/401/429 などコードとメッセージ、トレース方法を明記

<!-- Frontend が関係する場合 -->
**Frontend**
- ページ: `/path`
- イベント: 「操作」→ `関数()` → 成功で `/next` へ遷移 / 失敗で ErrorToast 表示
- UI 状態: loading/disabled、エラー表示メッセージ、フォーカス制御

<!-- DB 変更がある場合 -->
**DB / Migration**
- 対象テーブル: `table_name`
- 変更内容:
  | カラム | 型 | NULL | デフォルト | 制約/インデックス | 備考 |
  | --- | --- | --- | --- | --- | --- |
- 追加/変更制約:
- seed データ:
  | 用途 | 件数 | 内容 |
  | --- | --- | --- |

<!-- 監視・観測が必要な場合 -->
**監視/観測**
- 追加すべきメトリクス、ログ、アラート条件、既存ダッシュボードへの反映有無

**やること**
- [ ] Backend: ドメインロジック/ユースケース
- [ ] Backend: HTTP 層と Wire 配線
- [ ] Frontend: ページ実装と UI 文言
- [ ] QA/Docs: 受け入れ手順やモニタリング設定の追記

**完了条件**
- [ ] `make test-unit` が通過する
- [ ] 受け入れ観点が満たされる
- [ ] ドキュメント更新 (該当箇所)

**テスト (受け入れ観点)**
- 正常系: 〇〇
- 異常系: 〇〇
- 境界値: 〇〇（該当する場合）

**リスク / 相談事項**
- 不確定要素やボトルネックになりそうな点

**参考**
- 関連チケット、設計資料、ADR リンクなど

---

チケット出力後、末尾に推奨コミットメッセージを添える（例: `feat(go-backend): add rate limiting middleware (#<issue>)`）。

---

## GitHub Issue 登録

チケットを出力したら、**GitHub Issue として登録するかユーザーに確認する**。

承認された場合は以下を実行すること：

```bash
gh issue create \
  --title "<サマリの1行テキスト>" \
  --body "<チケット全文（Markdown）>"
```

- Issue タイトルは **サマリ**セクションの1行テキストをそのまま使用する。
- Issue 作成後、発行された Issue URL をユーザーに伝える。
