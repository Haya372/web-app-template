#\!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

if git diff --cached --name-only | grep -q "^apps/react-frontend/"; then
  cd apps/react-frontend && pnpm lint-staged
  pnpm lint
  pnpm build
fi
