// Stub for @connectrpc/connect-node — replaced by real package after pnpm install
// Used only in Vitest environment (see vitest.config.ts resolve.alias).
export const createConnectTransport = (): Record<string, unknown> => ({});
