title = "gitleaks config"

[extend]
# デフォルトルールセット（240以上のシークレットパターン）を継承する。
# regex/paths による一括除外は検知漏れリスクがあるため原則使用しない。
# false positive は .gitleaksignore で fingerprint 単位に個別管理する（ADR-0013参照）。
useDefault = true
